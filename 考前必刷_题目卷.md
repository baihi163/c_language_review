# 结课练习一 · 考前必刷 · 题目卷

> 共 **85 题**：单选 50 + 多选 20 + 判断 15  
> **本文件不含答案**。做完后对照 [考前必刷_答案解析.md](考前必刷_答案解析.md)  
> 单选 1~177 完整题面/截图见 [结课练习一.md](结课练习一.md)；178~334 在超星卷面或该文档快速答案表

---

## 一、单选必刷 50 题

> 按 `#1~50` 顺序做。表中「考点」仅作定位，不含答案。

| # | 卷面题号 | 考点提示 |
|---|----------|----------|
| 1 | 4 | 指针作形参能否交换两个指针变量 |
| 2 | 7 | 用指针交换两个字符串首字符 |
| 3 | 17 | `*p2=*p1` 与 `p2=p1` 的区别 |
| 4 | 12 | 连续关系运算 `a>b>c` 是否合法 |
| 5 | 31 | `while(k=0)` 循环执行次数 |
| 6 | 48 | `strlen` 与 `sizeof` 对字符串 |
| 7 | 57 | `int *p[3]` 是指针数组还是数组指针 |
| 8 | 65 | `s[1]` 是地址还是元素值 |
| 9 | 66 | 指针复制字符串是否复制 `\0` |
| 10 | 74 | 逗号表达式的最终值 |
| 11 | 90 | 字符串常量赋给 `char*` 与 `char[]` |
| 12 | 103 | for 循环更新表达式与打印顺序 |
| 13 | 108 | 双重循环变量追踪 |
| 14 | 111 | 数组名作实参 vs 元素作实参 |
| 15 | 117 | 逻辑与 `&&` 短路 |
| 16 | 133 | `%d` 打印字符的结果 |
| 17 | 141 | `static` 局部变量跨调用累加 |
| 18 | 153 | 宏 `#define` 无脑替换 |
| 19 | 157 | `%o` `%x` 输出格式 |
| 20 | 158 | `while(*y++)` 求字符串长度 |
| 21 | 159 | break 时循环变量取值 |
| 22 | 166 | `(c-1>=d)==1` 表达式求值 |
| 23 | 168 | 前缀/后缀与 if 组合输出 |
| 24 | 176 | 指针偏移 `*(ptr+2)` 的值 |
| 25 | 177 | 两个 `scanf` 格式串差异 |
| 26 | 190 | 指针解引用给变量赋值 |
| 27 | 198 | 函数缺返回类型 |
| 28 | 214 | 双重 for 嵌套输出 |
| 29 | 215 | 用指针访问 `x[2]` |
| 30 | 221 | 局部变量与同名全局变量 |
| 31 | 238 | `static` 变量累加 |
| 32 | 241 | 逗号表达式与 `++` |
| 33 | 251 | `z=x++` 赋值顺序 |
| 34 | 298 | 字符与 `%d` 输出 |
| 35 | 299 | `*ps+4` 指针算术 |
| 36 | 300 | `strlen("cd")` |
| 37 | 169 | `main` 函数位置 |
| 38 | 170 | 函数首部写法 |
| 39 | 171 | `++*x` 运算顺序 |
| 40 | 178 | `scanf` 三个 `%d` 的地址 |
| 41 | 297 | 指针交换实现逆序 |
| 42 | 305 | `char *aa[2]` 含义 |
| 43 | 308 | 函数实参类型匹配 |
| 44 | 309 | 函数原型写法 |
| 45 | 310 | 复合赋值与 `%` 表达式 |
| 46 | 311 | 语句是否缺分号 |
| 47 | 312 | `static` 与局部同名变量 |
| 48 | 318 | 奇数下标元素求和 |
| 49 | 321 | `z--` 与 `--` 组合 |
| 50 | 323 | 未初始化变量求和 |

**加练**：333、334（见答案解析）

---

## 二、多选必刷 20 题

### 多选 1 · 卷面 335

以下能正确定义二维数组的语句是（ ）

- A. `int a[2][2]={{1},{2}};`
- B. `int a[][2]={{1,2},{3,4}};`
- C. `int a[][]={1,2,3,4};`
- D. `int a[2][2]={{1},{2},{3}};`

---

### 多选 2 · 卷面 336

设有 `int a[6]={2,4,6,8,5,7}, *p=a;`，依次输出数组元素，能完成此操作的是（ ）

- A. `for(i=0;i<6;i++) printf("%d ", *p++);`
- B. `for(i=0;i<6;i++) printf("%d ", *(p+i));`
- C. `for(i=0;i<6;i++) printf("%d ", *p+i);`
- D. `for(i=0;i<6;i++) printf("%d ", *(p++));`

---

### 多选 3 · 卷面 340

下面各语句中，**不能**正确进行字符串赋值操作的是（ ）

- A. `char *s; s = "ABCDE";`
- B. `char s[8]="Bei jing!";`（超长）
- C. `char s[8]; s={"Bei jing"};`
- D. `char *s; s = "Beijing";`

---

### 多选 4 · 卷面 345

以下**不能**正确定义字符串的语句是（ ）

- A. `char str[]={"\x43"};`
- B. `char str='';`
- C. `char str[]={'\064'};`
- D. `char str[]="\0";`

---

### 多选 5 · 卷面 349

以下正确的定义语句是（ ）

- A. `int x[3][3]={{1},{1,2,3}};`
- B. `int x[4][3]={{1,2,3},{1,2,5},{1,2,5},{1,2,5}};`
- C. `int x[][3]={1,2,3,4};`
- D. `int x[4][]={{1,2,3},{1,2,3},{1,2,3},{1,2,3}};`

---

### 多选 6 · 卷面 350

对函数嵌套的叙述中，**错误**的叙述是（ ）

- A. 函数定义和调用均可以嵌套
- B. 函数定义不可以嵌套，但函数调用可以嵌套
- C. 函数定义可以嵌套，但函数调用不能嵌套
- D. 函数定义和调用均不能嵌套

---

### 多选 7 · 卷面 356

若变量均已正确定义并赋值，以下表达式**不符合** C 语言语法的是（ ）

- A. `a=a+7=c+b`
- B. `a=b+1`
- C. `(int)18.5%3`
- D. `a=b=c+2`

---

### 多选 8 · 卷面 361

以下**不能**正确定义二维数组的是（ ）

- A. `int a[2][3];`
- B. `int a[][3]={};`
- C. `int a[2][3]={{1},{2},{3,4}};`
- D. `int a[][3]={{1,2,3}};`

---

### 多选 9 · 卷面 342

能把字符串 `Hello!` 赋给数组的语句是（ ）

- A. `char b[10]; b="Hello!";`
- B. `char b[10]; strcpy(b,"Hello!");`
- C. `char b[10]="Hello!";`
- D. `char b[10]={'H','e','l','l','o','!'};`

---

### 多选 10 · 卷面 355

设 `p1`、`p2` 为指向同一 `int` 数组的指针，`k` 为 `int`，以下能正确执行的是（ ）

- A. `p1=p2;`
- B. `k=*p1+*p2;`
- C. `p2=k;`
- D. `k=*p1*(*p2);`

---

### 多选 11 · 卷面 367

`sizeof(double)` 是（ ）

- A. 一种函数调用
- B. 一个双精度型表达式
- C. 一个整型表达式
- D. `sizeof` 是运算符

---

### 多选 12 · 卷面 374

若有 `int aa[8];`，以下表达式中能代表 `aa[1]` 的地址的是（ ）

- A. `&aa[1]`
- B. `aa+1`
- C. `&aa[0]++`
- D. `&aa[0]+1`

---

### 多选 13 · 卷面 385

设有定义：

```c
int a=0;
int *p=&a;
char c='A';
#define d 2
```

下面语句中正确的是（ ）

- A. `c++;`
- B. `b++;`
- C. `a++;`
- D. `d++;`

---

### 多选 14 · 卷面 389

设有 `int a=1,b=2,*p1=&a,*p2=&b;`，以下**不能**使指针 `p1` 指向变量 `b` 的是（ ）

- A. `p1=*p2`
- B. `*p1=p2`
- C. `*p1=*p2`
- D. `p1=p2`

---

### 多选 15 · 卷面 363

以下所列 C 语言常量中，正确的是（ ）

- A. `'\72'`
- B. `2L`
- C. `0xFF`
- D. `1.2e0.5`

---

### 多选 16 · 卷面 375

以下叙述中**错误**的是（ ）

- A. 宏名必须用大写字母表示
- B. 在源文件的一行上只能有一条预处理命令
- C. 宏替换不占有程序的运行时间
- D. 预处理命令行必须位于源文件的开头

---

### 多选 17 · 卷面 337

以下对一维整型数组的**错误**定义是（ ）

- A. `int y[10];`
- B. `int n;`
- C. `int n=10, y[n];`
- D. `#define SIZE 10` / `int y[SIZE];`

---

### 多选 18 · 卷面 346

设有 `char p[]={'1','2','3'}; *q=p;`，以下能计算出 `char` 型变量所占字节数的表达式是（ ）

- A. `sizeof(*q)`
- B. `sizeof(p[0])`
- C. `sizeof(p)`
- D. `sizeof(char)`

---

### 多选 19 · 卷面 373

以下选项中，合法的常量是（ ）

- A. `1.234e0.4`
- B. `1.234e04`
- C. `e+4`
- D. `1.234e-2`

---

### 多选 20 · 卷面 393

设 `c1`、`c2` 均是 `char` 型变量，以下正确的函数调用为（ ）

- A. `getchar(c1);`
- B. `putchar("\n");`
- C. `putchar(c1);`
- D. `printf("%c,%c",c1,c2);`

---

## 三、判断必刷 15 题

> 判断对错：**A**=对 · **B**=错

| # | 卷面题号 | 判断句 |
|---|----------|--------|
| 1 | 406 | 空串一定比空格打头的字符串小 |
| 2 | 430 | C 语言没有专门的逻辑类型，用 0 和非 0 表示假和真 |
| 3 | 434 | 用 do-while 语句构成的循环，在 while 后的表达式为非零时结束循环 |
| 4 | 453 | 当调用函数时，实参是一个数组名，则向函数传送的是数组每个元素中的值 |
| 5 | 424 | 用户定义的函数中若没有 return 语句，则应当定义函数为 void 类型 |
| 6 | 426 | C 程序的基本组成单位是函数 |
| 7 | 442 | C 语言中，宏替换没有数据类型限制 |
| 8 | 468 | C 程序中的 `#include` 和 `#define` 均不是 C 语句 |
| 9 | 444 | C 语言中，每个函数都可以被其它函数调用（包括 main 函数） |
| 10 | 415 | 函数的定义不可以嵌套，但函数的调用可以嵌套 |
| 11 | 419 | 若用数组名作为实参，则传递给形参的是数组 |
| 12 | 402 | 函数的形参和实参分别占用不同的存储单元 |
| 13 | 436 | `sizeof(double)` 是一种函数调用 |
| 14 | 417 | `a=b+c=1` 是正确的赋值表达式 |
| 15 | 462 | 若 t 为 double 型，表达式 `t=1,t+5,t++` 的值是 1.0 |

---

*刷题顺序：单选 50 → 多选 20 → 判断 15*
