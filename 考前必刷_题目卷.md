# 结课练习一 · 考前必刷 · 题目卷

> 共 **85 题**：单选 50 + 多选 20 + 判断 15  
> **本文件不含答案**。做完后对照 [考前必刷_答案解析.md](考前必刷_答案解析.md)  
> 带 📷 的题在 [结课练习一.md](结课练习一.md) 或 `images/结课练习一/` 有截图

---

## 一、单选必刷 50 题

### 单选 1 · 卷面 4

有以下程序，运行后的输出结果是（ ）

```c
void fun(char *a, char *b)
{ a = b;
  (*a)++; }
main()
{ char c1 = 'A', c2 = 'a', *p1, *p2;
  p1 = &c1;
  p2 = &c2;
  fun(p1, p2);
  printf("%c%c\n", c1, c2); }
```

- A. aa
- B. Bb
- C. Ab
- D. Aa

---

### 单选 2 · 卷面 7 📷

有以下程序，程序执行后的输出结果是（ ）

```c
void swap(char *x, char *y)
{ char t;
  t=*x; *x=*y; *y=t; }
main()
{ char s1[]="abc", s2[]="123";
  swap(s1, s2);
  printf("%s, %s\n", s1, s2); }
```

- A. abc, 123
- B. 1bc, a23
- C. 321, cba
- D. 123, abc

---

### 单选 3 · 卷面 17 📷

以下函数的功能是（ ）

```c
fun (char* p2, char* p1)
{ while ((*p2=*p1) != '\0')
  { p1++; p2++; }
}
```

- A. 对 p1 和 p2 两个指针所指字符串进行比较
- B. 将 p1 所指字符串复制到 p2 所指内存空间
- C. 检查 p1 和 p2 两个指针所指字符串中是否有 `'\0'`
- D. 将 p1 所指字符串的地址赋给指针 p2

---

### 单选 4 · 卷面 12

为表示关系 x≥y≥z，应使用 C 语言表达式（ ）。

- A. `(x>=y)&&(y>=z)`
- B. `(x>=y>=z)`
- C. `(x>=y)||(y>=z)`
- D. `(x>=y)AND(y>=z)`

---

### 单选 5 · 卷面 31 📷

下述循环的循环次数是（ ）。

```c
int k=2;
while(k=0) printf("%d",k);
k--;
printf("\n");
```

- A. 1次
- B. 2次
- C. 无限次
- D. 0次

---

### 单选 6 · 卷面 48 📷

以下程序的输出结果是（ ）

```c
main()
{ char str[12]={'h','e','l','l','o','\0','g'};
  printf("%d\n", strlen(str)); }
```

- A. 6
- B. 11
- C. 12
- D. 7

---

### 单选 7 · 卷面 57

若有定义 `int *p[3];`，则以下叙述中正确的是（ ）

- A. 定义了一个指针数组 p，该数组含有三个元素，每个元素都是基类型为 int 的指针
- B. 定义了一个基类型为 int 的指针变量 p，该变量具有三个指针
- C. 定义了一个名为 p 的整型数组，该数组含有三个 int 类型元素
- D. 定义了一个指向一维数组的指针变量 p，所指一维数组具有三个 int 类型元素

---

### 单选 8 · 卷面 65 📷

有定义语句 `char s[10];`，若要从终端输入 5 个字符，错误的输入语句是（ ）

- A. `scanf("%s", s+1);`
- B. `gets(s);`
- C. `scanf("%s", s[1]);`
- D. `gets(&s[0]);`

---

### 单选 9 · 卷面 66 📷

以下与库函数 `strcpy(char *p1, char *p2)` 功能不相等的程序段是（ ）。

- A. `while ((*p1=*p2) != '\0') { p1++; p2++; }`
- B. `while (*p2) *p1++ = *p2++;`
- C. `while ((*p1++=*p2++) != '\0');`
- D. `while (*p1++=*p2++);`

---

### 单选 10 · 卷面 74 📷

若已定义 `int x=100, y=200`，下面程序片段的输出结果是（ ）

```c
printf("%d", (x,y));
```

- A. 100
- B. 200
- C. 输出格式符不够，输出不确定的值
- D. 100 200

---

### 单选 11 · 卷面 90 📷

以下语句或语句组中，能正确进行字符串赋值的是（ ）

- A. `char s[10]; s="right!";`
- B. `char *sp; *sp="right!";`
- C. `char s[10]; *s="right!";`
- D. `char *sp="right!";`

---

### 单选 12 · 卷面 103 📷

以下程序的输出结果是（ ）

```c
main()
{
    int x=10, y=10, i;
    for(i=0; x>8; y=++i)
        printf("%d, %d ", x--, y);
}
```

- A. 10 10 9 0
- B. 9 8 7 6
- C. 10 10 9 1
- D. 10 1 9 2

---

### 单选 13 · 卷面 108 📷

程序段的输出结果是（ ）

```c
main()
{
    int n[2],i,j,k;
    for(i=0;i<2;i++)
        n[i]=0;
    k=2;
    for(i=0;i<k;i++)
        for(j=0;j<k;j++)
            n[j]=n[i]+1;
    printf("%d\n",n[0]);
}
```

- A. 2
- B. 不确定的值
- C. 3
- D. 1

---

### 单选 14 · 卷面 111 📷

下列程序运行后的输出结果是（ ）

```c
void swap1(int a[]) { int t=a[0]; a[0]=a[1]; a[1]=t; }
void swap2(int x, int y) { int t=x; x=y; y=t; }
main()
{
    int a[]={3,5}, b[]={3,5};
    swap1(a);
    swap2(b[0], b[1]);
    printf("%d %d %d %d", a[0],a[1],b[0],b[1]);
}
```

- A. 5 3 3 5
- B. 3 5 3 5
- C. 5 5 3 3
- D. 3 5 5 3

---

### 单选 15 · 卷面 117

设有 `int a=1, b=2, c=3, d=4, m=3, n=3;`，执行 `(m=a>b)&&(n=c>d)` 后 n 的值为（ ）。

- A. 1
- B. 2
- C. 0
- D. 3

---

### 单选 16 · 卷面 133

有以下程序，则下述叙述中正确的是（ ）

```c
main()
{ char a1='M', a2='m';
  printf("%d\n", (a1, a2)); }
```

- A. 格式说明符不足，编译出错
- B. 程序输出小写字母 m
- C. 程序输出大写字母 M
- D. 输出 m 的 ASCII 码值（109）

---

### 单选 17 · 卷面 141 📷

以下程序运行后，输出结果是（ ）

```c
func(int a, int b)
{ static int m=0,i=2;
  i+=m+1;
  m=i+a+b;
  return(m); }
main()
{ int k=4,m=1,p;
  p=func(k,m);
  printf("%d,",p);
  p=func(k,m);
  printf("%d\n",p); }
```

- A. 8,16
- B. 8,8
- C. 8,15
- D. 8,17

---

### 单选 18 · 卷面 153 📷

有以下程序，运行后的输出结果是（ ）

```c
#define P 3
#define F(x) P*x*x
main()
{ printf("%d\n", F(3+5)); }
```

- A. 25
- B. 编译出错
- C. 192
- D. 29

---

### 单选 19 · 卷面 157 📷

下述程序的输出结果是（ ）。

```c
main()
{ int k=11;
  printf("k=%d,k=%o,k=%x\n",k,k,k); }
```

- A. k=11,k=13,k=b
- B. k=11,k=12,k=11
- C. k=11,k=15,k=13
- D. k=11,k=013,k=0xb

---

### 单选 20 · 卷面 158

下面函数的功能是（ ）

```c
int fun(char *s)
{ char *y=s;
  while(*y++);
  return(y-s-1); }
```

- A. 比较两个字符串的大小
- B. 求字符串的长度
- C. 将字符串 s 复制到字符串 y
- D. 将字符串 s 连接到字符串 y 后面

---

### 单选 21 · 卷面 159

以下程序运行后的输出结果是（ ）

```c
main()
{ int a=1,b;
  for(b=1;b<=10;b++)
  {
    if(a>=8) break;
    if(a%2==1) { a+=5; continue; }
    a-=3;
  }
  printf("%d\n",b); }
```

- A. 6
- B. 5
- C. 4
- D. 3

---

### 单选 22 · 卷面 166

有以下程序，执行后输出的结果是（ ）

```c
main()
{
    int a=5, b=4, c=3, d=2;
    if(a>b>c)
        printf("%d\n", d);
    else if((c-1>=d)==1)
        printf("%d\n", d+1);
    else
        printf("%d\n", d+2);
}
```

- A. 编译时有错，无结果
- B. 3
- C. 4
- D. 2

---

### 单选 23 · 卷面 168

两次运行下面的程序，如果从键盘上分别输入 6 和 4，则输出结果是（ ）

```c
main()
{
    int x;
    scanf("%d", &x);
    if(x++ > 5) printf("%d", x);
    else printf("%d\n", x--);
}
```

- A. 6和3
- B. 7和4
- C. 7和5
- D. 7和5

---

### 单选 24 · 卷面 176

以下程序的输出结果是（ ）

```c
main()
{ int x[]={15,10,17,21,16,22},*pt;
  pt=x+2;
  printf("%d\n",*(pt+2)); }
```

- A. 数组元素 x[4] 地址
- B. 数组元素 x[2] 的地址
- C. 21
- D. 16

---

### 单选 25 · 卷面 177

对于下述语句，若将 10 赋给 k1 和 k3，将 20 赋给 k2 和 k4，则应按下列（ ）方式输入数据。（↵ 表示回车）

```c
int k1,k2,k3,k4;
scanf("%d %d",&k1,&k2);
scanf("%d,%d",&k3,&k4);
```

- A. 10 20↵ 10,20↵
- B. 10 20↵ 10,20↵
- C. 10 20↵ 10 20↵
- D. 10 20↵ 1020↵

---

### 单选 26 · 卷面 190 📷

若有说明：`int i, j=2, *p=&i;`，则能完成 `i=j` 赋值功能的语句是（ ）

- A. `i=**p;`
- B. `i=*p;`
- C. `*p=j;`
- D. `i=&j;`

---

### 单选 27 · 卷面 198 📷

以下函数值的类型是（ ）

```c
fun(float x)
{ float y;
  y=3*x-4;
  return y; }
```

- A. 不确定
- B. float
- C. void
- D. int

---

### 单选 28 · 卷面 214 📷

有以下程序，运行结果是（ ）

```c
main()
{ int i, j;
  for(i=3; i>=1; i--)
  { for(j=1; j<=2; j++) printf("%d ", i+j);
    printf("\n"); }
}
```

- A. `2 3 4` / `3 4 5`（两行）
- B. `2 3` / `3 4` / `4 5`（三行）
- C. `4 5` / `3 4` / `2 3`（三行）
- D. `4 3 2` / `5 4 3`（两行）

---

### 单选 29 · 卷面 215 📷

若有定义语句 `float x[6];`，则以下对 x 数组元素的**正确引用形式**是（ ）

- A. `*(x+2)`
- B. `&(x+2)`
- C. `*x[6]`
- D. `x+2`

---

### 单选 30 · 卷面 221 📷

下述程序输出的结果是（ ）

```c
int s=13;
int fun(int x, int y)
{ int s=3;
  return(x*y-s); }
main()
{ int m=7, n=5;
  printf("%d\n", fun(m,n)/s); }
```

- A. 7
- B. 1
- C. 2
- D. 10

---

### 单选 31 · 卷面 238 📷

下面程序的输出是（ ）

```c
fun3(int x)
{ static int a=3;
  a+=x;
  return(a); }
main()
{ int k=2, m=1, n;
  n=fun3(k);
  n=fun3(m);
  printf("%d\n", n); }
```

- A. 6
- B. 4
- C. 3
- D. 9

---

### 单选 32 · 卷面 241 📷

有如下程序，运行的输出结果是（ ）

```c
main()
{ int y=3, x=3, z=1;
  printf("%d %d\n", (++x, y++), z+2); }
```

- A. 3 4
- B. 4 3
- C. 3 3
- D. 4 2

---

### 单选 33 · 卷面 251

（卷面见超星 178~334 段）

设有 `int x=1, z;`，执行 `z=x++;` 后，z 和 x 的值分别是（ ）。

- A. 1, 2
- B. 2, 1
- C. 2, 2
- D. 1, 1

---

### 单选 34 · 卷面 298

（卷面见超星 178~334 段）

```c
char c1='A', c2='B';
printf("%d,%d\n", c1, c2);
```

输出结果是（ ）。

- A. A,B
- B. 65,66
- C. 66,65
- D. B,A

---

### 单选 35 · 卷面 299

（卷面见超星 178~334 段）

设有 `char s[]="abcde", *ps=s;`，表达式 `*ps+4` 的值是（ ）。

- A. 字符 `'e'`
- B. 字符 `'a'`
- C. 地址 ps+4
- D. 5

---

### 单选 36 · 卷面 300

（卷面见超星 178~334 段）

`strlen("cd")` 的值是（ ）。

- A. 0
- B. 1
- C. 3
- D. 2

---

### 单选 37 · 卷面 169 📷

在一个 C 语言程序中（ ）

- A. main 函数必须出现在固定位置
- B. main 函数必须出现在所有函数之前
- C. main 函数可以在任何地方出现
- D. main 函数必须出现在所有函数之后

---

### 单选 38 · 卷面 170 📷

以下所列的各函数首部中，正确的是（ ）

- A. `Sub play(a as integer, b as integer)`
- B. `void play(int a, b)`
- C. `void play(var a:integer, var b:integer)`
- D. `void play(int a, int b)`

---

### 单选 39 · 卷面 171 📷

下面程序的输出是（ ）

```c
void prtv(int *x)
{ printf("%d\n", ++*x); }
main()
{ int a=25;
  prtv(&a); }
```

- A. 25
- B. 23
- C. 24
- D. 26

---

### 单选 40 · 卷面 178 📷

x、y、z 被定义为整型变量，若从键盘给 x、y、z 输入数据，正确的输入语句是（ ）

- A. `scanf("%d%d%d", x, y, z);`
- B. `scanf("%d %d %d", &x, &y, &z);`
- C. `scanf("%d,%d,%d", &x, &y, &z);`
- D. `scanf("%d%d%d", &x, &y, &z);`

---

### 单选 41 · 卷面 297 📷

有以下程序，运行后的输出结果是（ ）

```c
void f(int *x, int *y)
{ int t;
  t=*x; *x=*y; *y=t; }
main()
{ int a[8]={1,2,3,4,5,6,7,8}, i, *p, *q;
  p=a; q=&a[7];
  while(p<q)
  { f(p, q);
    p++; q--; }
  for(i=0; i<8; i++)
    printf("%d,", a[i]); }
```

- A. 8,7,6,5,4,3,2,1,
- B. 8,2,3,4,5,6,7,1,
- C. 5,6,7,8,1,2,3,4,
- D. 1,2,3,4,5,6,7,8,

---

### 单选 42 · 卷面 305 📷

设有如下定义：`char *aa[2]={"abcd","ABCD"};`，则以下说法中正确的是（ ）

- A. aa 数组各元素的值分别是 "abcd" 和 "ABCD"
- B. aa 是指针变量，它指向含有两个数组元素的字符型一维数组
- C. aa 数组的两个元素分别存放的是含有 4 个字符的一维字符数组的首地址
- D. aa 数组的两个元素中各自存放了字符串 "abcd" 和 "ABCD" 的首地址

---

### 单选 43 · 卷面 308 📷

设有如下调用语句，则 fun 的函数首部中**错误**的是（ ）

```c
main()
{ ... int a[50], n;
  ... fun(n, &a[9]);
  ... }
```

- A. `void fun(int p, int *s)`
- B. `void fun(int n, int)`
- C. `void fun(int s, int h[41])`
- D. `void fun(int m, int x[])`

---

### 单选 44 · 卷面 309 📷

以下程序运行后的输出结果是（ ）

```c
int a=3;
main()
{ int s=0;
  { int a=5;
    s+=a++; }
  s+=a++;
  printf("%d\n", s); }
```

- A. 11
- B. 7
- C. 10
- D. 8

---

### 单选 45 · 卷面 310 📷

假定有以下变量定义：`int k=7, x=12;`，则能使值为 3 的表达式是（ ）

- A. `x%=k-k%5`
- B. `x%=(k-k%5)`
- C. `x%=(k%5)`
- D. `(x%=k)-(k%=5)`

---

### 单选 46 · 卷面 311 📷

若 k 是 int 型变量，且有下面的程序片段，输出结果是（ ）

```c
k=-3;
if(k<=0) printf("####")
else  printf("&&&&");
```

- A. `####`
- B. `&&&&`
- C. `####&&&&`
- D. 有语法错误，无输出结果

---

### 单选 47 · 卷面 312 📷

以下程序的运行结果是（ ）

```c
int a=1;
int f(int c)
{ static int a=2;
  c=c+1;
  return (a++)+c; }
main()
{ int i, k=0;
  for(i=0; i<2; i++)
  { int a=3;
    k+=f(a); }
  k+=a;
  printf("%d\n", k); }
```

- A. 17
- B. 14
- C. 15
- D. 16

---

### 单选 48 · 卷面 318 📷

以下程序执行后输出的结果是（ ）

```c
main()
{ int p[7]={11,13,14,15,16,17,18}, i=0, k=0;
  while(i<7 && p[i]%2)
  { k=k+p[i]; i++; }
  printf("%d\n", k); }
```

- A. 24
- B. 56
- C. 45
- D. 58

---

### 单选 49 · 卷面 321 📷

执行以下程序后的输出结果是（ ）

```c
main()
{ int x=0, y=5, z=3;
  while(z-->0 && ++x<5)
    y=y-1;
  printf("%d,%d,%d\n", x, y, z); }
```

- A. 3,2,0
- B. 3,2,-1
- C. 5,-2,-5
- D. 4,3,-1

---

### 单选 50 · 卷面 323 📷

以下程序执行后 sum 的值是（ ）

```c
main()
{ int i, sum;
  for(i=1; i<6; i++)
    sum+=i;
  printf("%d\n", sum); }
```

- A. 不确定
- B. 0
- C. 15
- D. 14

---

**加练**：333、334（见 [考前必刷_答案解析.md](考前必刷_答案解析.md)）

---

## 二、多选必刷 20 题

### 多选 1 · 卷面 335

以下能正确定义二维数组的语句是（ ）

- A. `int a[2][2]={{1},{2}};`
- B. `int a[][2]={{1,2},{3,4}};`
- C. `int a[][]={1,2,3,4};`
- D. `int a[2][2]={{1},{2},{3}};`

---

### 多选 2 · 卷面 336

设有 `int a[6]={2,4,6,8,5,7}, *p=a;`，依次输出数组元素，能完成此操作的是（ ）

- A. `for(i=0;i<6;i++) printf("%d ", *p++);`
- B. `for(i=0;i<6;i++) printf("%d ", *(p+i));`
- C. `for(i=0;i<6;i++) printf("%d ", *p+i);`
- D. `for(i=0;i<6;i++) printf("%d ", *(p++));`

---

### 多选 3 · 卷面 340

下面各语句中，**不能**正确进行字符串赋值操作的是（ ）

- A. `char *s; s = "ABCDE";`
- B. `char s[8]="Bei jing!";`（超长）
- C. `char s[8]; s={"Bei jing"};`
- D. `char *s; s = "Beijing";`

---

### 多选 4 · 卷面 345

以下**不能**正确定义字符串的语句是（ ）

- A. `char str[]={"\x43"};`
- B. `char str='';`
- C. `char str[]={'\064'};`
- D. `char str[]="\0";`

---

### 多选 5 · 卷面 349

以下正确的定义语句是（ ）

- A. `int x[3][3]={{1},{1,2,3}};`
- B. `int x[4][3]={{1,2,3},{1,2,5},{1,2,5},{1,2,5}};`
- C. `int x[][3]={1,2,3,4};`
- D. `int x[4][]={{1,2,3},{1,2,3},{1,2,3},{1,2,3}};`

---

### 多选 6 · 卷面 350

对函数嵌套的叙述中，**错误**的叙述是（ ）

- A. 函数定义和调用均可以嵌套
- B. 函数定义不可以嵌套，但函数调用可以嵌套
- C. 函数定义可以嵌套，但函数调用不能嵌套
- D. 函数定义和调用均不能嵌套

---

### 多选 7 · 卷面 356

若变量均已正确定义并赋值，以下表达式**不符合** C 语言语法的是（ ）

- A. `a=a+7=c+b`
- B. `a=b+1`
- C. `(int)18.5%3`
- D. `a=b=c+2`

---

### 多选 8 · 卷面 361

以下**不能**正确定义二维数组的是（ ）

- A. `int a[2][3];`
- B. `int a[][3]={};`
- C. `int a[2][3]={{1},{2},{3,4}};`
- D. `int a[][3]={{1,2,3}};`

---

### 多选 9 · 卷面 342

能把字符串 `Hello!` 赋给数组的语句是（ ）

- A. `char b[10]; b="Hello!";`
- B. `char b[10]; strcpy(b,"Hello!");`
- C. `char b[10]="Hello!";`
- D. `char b[10]={'H','e','l','l','o','!'};`

---

### 多选 10 · 卷面 355

设 `p1`、`p2` 为指向同一 `int` 数组的指针，`k` 为 `int`，以下能正确执行的是（ ）

- A. `p1=p2;`
- B. `k=*p1+*p2;`
- C. `p2=k;`
- D. `k=*p1*(*p2);`

---

### 多选 11 · 卷面 367

`sizeof(double)` 是（ ）

- A. 一种函数调用
- B. 一个双精度型表达式
- C. 一个整型表达式
- D. `sizeof` 是运算符

---

### 多选 12 · 卷面 374

若有 `int aa[8];`，以下表达式中能代表 `aa[1]` 的地址的是（ ）

- A. `&aa[1]`
- B. `aa+1`
- C. `&aa[0]++`
- D. `&aa[0]+1`

---

### 多选 13 · 卷面 385

设有定义：

```c
int a=0;
int *p=&a;
char c='A';
#define d 2
```

下面语句中正确的是（ ）

- A. `c++;`
- B. `b++;`
- C. `a++;`
- D. `d++;`

---

### 多选 14 · 卷面 389

设有 `int a=1,b=2,*p1=&a,*p2=&b;`，以下**不能**使指针 `p1` 指向变量 `b` 的是（ ）

- A. `p1=*p2`
- B. `*p1=p2`
- C. `*p1=*p2`
- D. `p1=p2`

---

### 多选 15 · 卷面 363

以下所列 C 语言常量中，正确的是（ ）

- A. `'\72'`
- B. `2L`
- C. `0xFF`
- D. `1.2e0.5`

---

### 多选 16 · 卷面 375

以下叙述中**错误**的是（ ）

- A. 宏名必须用大写字母表示
- B. 在源文件的一行上只能有一条预处理命令
- C. 宏替换不占有程序的运行时间
- D. 预处理命令行必须位于源文件的开头

---

### 多选 17 · 卷面 337

以下对一维整型数组的**错误**定义是（ ）

- A. `int y[10];`
- B. `int n;`
- C. `int n=10, y[n];`
- D. `#define SIZE 10` / `int y[SIZE];`

---

### 多选 18 · 卷面 346

设有 `char p[]={'1','2','3'}; *q=p;`，以下能计算出 `char` 型变量所占字节数的表达式是（ ）

- A. `sizeof(*q)`
- B. `sizeof(p[0])`
- C. `sizeof(p)`
- D. `sizeof(char)`

---

### 多选 19 · 卷面 373

以下选项中，合法的常量是（ ）

- A. `1.234e0.4`
- B. `1.234e04`
- C. `e+4`
- D. `1.234e-2`

---

### 多选 20 · 卷面 393

设 `c1`、`c2` 均是 `char` 型变量，以下正确的函数调用为（ ）

- A. `getchar(c1);`
- B. `putchar("\n");`
- C. `putchar(c1);`
- D. `printf("%c,%c",c1,c2);`

---

## 三、判断必刷 15 题

> 判断对错：**A**=对 · **B**=错

### 判断 1 · 卷面 406

空串一定比空格打头的字符串小。（ ）

- A. 对
- B. 错

---

### 判断 2 · 卷面 430

C 语言没有专门的逻辑类型，用 0 和非 0 表示假和真。（ ）

- A. 对
- B. 错

---

### 判断 3 · 卷面 434

用 do-while 语句构成的循环，在 while 后的表达式为非零时结束循环。（ ）

- A. 对
- B. 错

---

### 判断 4 · 卷面 453

当调用函数时，实参是一个数组名，则向函数传送的是数组每个元素中的值。（ ）

- A. 对
- B. 错

---

### 判断 5 · 卷面 424

用户定义的函数中若没有 return 语句，则应当定义函数为 void 类型。（ ）

- A. 对
- B. 错

---

### 判断 6 · 卷面 426

C 程序的基本组成单位是函数。（ ）

- A. 对
- B. 错

---

### 判断 7 · 卷面 442

C 语言中，宏替换没有数据类型限制。（ ）

- A. 对
- B. 错

---

### 判断 8 · 卷面 468

C 程序中的 `#include` 和 `#define` 均不是 C 语句。（ ）

- A. 对
- B. 错

---

### 判断 9 · 卷面 444

C 语言中，每个函数都可以被其它函数调用（包括 main 函数）。（ ）

- A. 对
- B. 错

---

### 判断 10 · 卷面 415

函数的定义不可以嵌套，但函数的调用可以嵌套。（ ）

- A. 对
- B. 错

---

### 判断 11 · 卷面 419

若用数组名作为实参，则传递给形参的是数组。（ ）

- A. 对
- B. 错

---

### 判断 12 · 卷面 402

函数的形参和实参分别占用不同的存储单元。（ ）

- A. 对
- B. 错

---

### 判断 13 · 卷面 436

`sizeof(double)` 是一种函数调用。（ ）

- A. 对
- B. 错

---

### 判断 14 · 卷面 417

`a=b+c=1` 是正确的赋值表达式。（ ）

- A. 对
- B. 错

---

### 判断 15 · 卷面 462

若 t 为 double 型，表达式 `t=1,t+5,t++` 的值是 1.0。（ ）

- A. 对
- B. 错

---

*刷题顺序：单选 50 → 多选 20 → 判断 15*
